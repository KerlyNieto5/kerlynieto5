#se necesita llenar el vector aleatorio,de los 10 numeros que se generan cuales tienen un solo digito y los demas ponerlos en 0
#los numeros que se guardan en el vector son edades y en otro vector decir sin mayores de edad o menores de edad
vector= 0

pares2=[x if x%2==0 else 0 for x in vector]
print(pares2)

undigito=(x if x<10 else 0 for x in vector)

print(undigito)

edad=['maryor' if x>=18 else 'menor' for x in vector]
print(edad)

